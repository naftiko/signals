---
layout: none
---          
[{% for link in site.data.links %}
    {
    "name": "{{ link.name }}",
    "description": "{{ link.description | strip_newlines }}",
    "url": "{{ link.url }}",
    "tags": {{ link.tags | jsonify }}
}{% unless forloop.last %},{% endunless %}{% endfor %}
]
